Definition

The Adapter Design Pattern is a structural design pattern that allows two incompatible interfaces to work together.

It acts as a bridge/translator between an existing class and the interface expected by the client.

Real-life example

Think about a mobile charger adapter:

Your phone expects a USB-C connection.
The power socket provides a different type of connection.
The adapter converts one interface into another so they can work together.